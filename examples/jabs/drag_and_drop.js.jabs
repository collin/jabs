:ready
  $#sortable
    .sortable 'li'

def sortable parent_selector
  :dragstart
    event.target
      .closest parent_selector
        .clone
          .css
            position: 'absolute'
            top: 5
          .appendTo document.body

  :drag
    event.dragProxy
      .css
        top: event.offsetY
        left: event.offsetX

  :dragend
    event.dragProxy 
      .remove

